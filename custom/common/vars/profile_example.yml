---
# list of default roles
default_roles:
  - apt
  - tmux
  - fonts
  - zsh

# custom variables
custom:
  git:
    name: ""
    email: ""

  dirs:
    dropbox: "{{ home }}/Dropbox"
    livebook: "{{ home }}/Dropbox/livebook"
    config: "{{ home }}/.config"
