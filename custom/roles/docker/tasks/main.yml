---
# TODO: run docker in userspace by default
# CURRENT_UID=$(id -u):$(id -g) docker
# command. add to alias,

- name: Add Docker GPG apt Key
  ansible.builtin.apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
    state: present
  become: yes
  become_user: root
  become_method: sudo

- name: Add Docker Repository
  ansible.builtin.apt_repository:
    repo: deb https://download.docker.com/linux/ubuntu focal stable
    state: present
  become: yes
  become_user: root
  become_method: sudo

- name: Install required system packages
  ansible.builtin.apt:
    pkg:
      - ca-certificates
      - gnupg
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: latest
    update_cache: true
  become: yes
  become_user: root
  become_method: sudo
