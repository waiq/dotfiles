home: "{{ ansible_env.HOME }}"

my_dir: "{{ home }}/.my"
my_dir_bin: "{{ my_dir }}/bin"
my_dir_notes: "{{ my_dir }}/notes"
my_dir_dotfiles: "{{ my_dir }}/dotfiles"
my_dir_env: "{{ my_dir }}/env"
my_dir_env_d: "{{ my_dir_env }}/env.d"

dir_config: "{{ home }}/.config"
dir_dev: "{{ home }}/dev"

git_name: "waiq"
