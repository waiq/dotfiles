---
- name: "Core commands"
  hosts: local
  vars: 
    install:
      git_pull: true
  vars_files:
    ./vars/main.yml
  roles:
    - role: ./roles/setup
      tags: setup
    - role: ./roles/update_tags
      tags: update-tags
